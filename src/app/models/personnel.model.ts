export interface Personnel {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    position: string;
    department: string;
    startDate: string; // Tarihleri şimdilik string olarak tutalım
}